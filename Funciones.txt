BACK:
    void initNavy();
    
    void initInvaders();
    
    moveNavy(int direction);
    
    moveInvaders(int x, int y);
    
    int shoot(int x, int y, int direction):
        Param x: posición del disparo en eje x.
        Param y: posición del disparo en eje y.
        Param direction: sentido en el que se mueve el disparo.
        Return: '1' si se impacto algo, '0' si no existió impacto.



FRONT:
    
    RASPI:

    ALLEGRO:
